services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data_buah:/app/data_buah
      - ./output:/app/output
      - ./app/database.json:/app/app/database.json
    environment:
      - PORT=5000
    restart: unless-stopped
